## File version (do NOT edit)
file-version: 1

## MySQL connection
mysql:
  host: localhost
  port: 3306
  database: map
  username: map
  password: password

## Socket IO connection
# FQDN or IP where the socket is hosted
hostname: localhost
# Port for the socket (default 8899). If proxy use 433 (Encrypted) or 80 (Un-encrypted)
port: 8899
# Token for communication (must be same here and on map backend .env file)        
token: token0001
